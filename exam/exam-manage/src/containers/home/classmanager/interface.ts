export interface IProps {
  props: any,
}
export interface IState {
  list: any[],
  classList: any[],
  classRoom: any[],
  studentUser: string,
  classRoomname: string,
  className: string,
  columns: any[],
  id: number,
  visible: boolean,
  text: string,
  pagination:{},
  pageSize:number,
}
